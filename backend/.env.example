# Bill Splitter - Environment Configuration
# Copy this file to .env and update with your values

# Environment mode (development or production)
MONGO_ENV=development

# MongoDB Root Credentials
DB_ROOT_USER=root
DB_ROOT_PASSWORD=change_me

# MongoDB Application Credentials
DB_USER=change_me
DB_PASSWORD=change_me
DB_NAME=change_me

# JWT Secret (use a long random string)
SECRET=change_me

# MongoDB Connection URLs
# For development
MONGO_URL_DEV=mongodb://DB_USER_HERE:DB_PASSWORD_HERE@template-mongo-dev:27017/DB_NAME_HERE

# For production (use same credentials as above)
MONGO_URL=mongodb://DB_USER_HERE:DB_PASSWORD_HERE@template-mongo:27017/DB_NAME_HERE

# Domain Configuration (for production SSL/HTTPS)
# Your domain name (e.g., example.com or subdomain.example.com)
DOMAIN_NAME=your-domain.com

# Login Password Hash
# Generate this by running: cd backend && npm run setup-password
# Example format: LOGIN_PASSWORD_HASH=$2b$10$abcdefghijklmnopqrstuvwxyz123456
# Note: Docker Compose may show a warning about undefined variables - this is harmless
# IMPORTANT: Never commit the actual .env file with this value to version control
LOGIN_PASSWORD_HASH=
